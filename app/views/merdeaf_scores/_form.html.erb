<%= form_with(model: merdeaf_score, local: true) do |form| %>
  <% if merdeaf_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(merdeaf_score.errors.count, "error") %> prohibited this merdeaf_score from being saved:</h2>

      <ul>
      <% merdeaf_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
